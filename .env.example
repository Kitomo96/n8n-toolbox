# =======================================================
#         Configuration for the n8n-Toolbox
# =======================================================
# Copy this file to .env and fill in your values.

# --- Caddy Configuration ---
# The full domain or subdomain Caddy should use for HTTPS.
# Caddy will automatically provision a certificate for this domain.
# Example: toolbox.my-awesome-project.com
CADDY_DOMAIN=

# --- Application Configuration ---
# The application version to be displayed at the /version endpoint.
APP_VERSION=3.2.0-final

# The maximum file size in megabytes that the application will accept for uploads or downloads.
MAX_FILE_SIZE_MB=50

# The log level for the application. Can be DEBUG, INFO, WARNING, ERROR.
# Use DEBUG for detailed troubleshooting, INFO for production.
LOG_LEVEL=INFO

# --- Performance Tuning ---
# The number of Uvicorn worker processes to run.
# A good starting point is (2 * number of CPU cores) + 1.
# Example for a 2-core VPS: (2 * 2) + 1 = 5
UVICORN_WORKERS=3

# --- Security (Future-Proofing) ---
# A secret API key to protect your endpoints.
# While not yet enforced, you can generate a strong random string here for when you enable it.
# Example: openssl rand -hex 32
API_KEY=